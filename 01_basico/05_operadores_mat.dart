main() {
  int a = 10 + 5; // 15 significa que se suman los dos valores
  a = 20 - 10; // 10 significa que se restan los dos valores
  a = 10 * 2; // 20 significa que se multiplican los dos valores

  double b = 10 / 2; // 5 significa que se dividen los dos valores
  b = 10.0 % 3; // 1 significa que se obtiene el residuo de la división
  b = -b; // -1 significa que se cambia el signo del valor

  int c = 10 ~/ 3; // 3 significa que se obtiene la parte entera de la división

  int d = 1;
  d++; // 2 significa que se incrementa en 1 el valor
  d--; // 1 significa que se decrementa en 1 el valor

  d += 2; // 3 significa que se incrementa en 2 el valor
  d -= 2; // 1 significa que se decrementa en 2 el valor
}
